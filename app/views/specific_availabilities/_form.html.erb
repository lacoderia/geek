<%= form_for(@specific_availability) do |f| %>
  <% if @specific_availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specific_availability.errors.count, "error") %> prohibited this specific_availability from being saved:</h2>

      <ul>
      <% @specific_availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tutor_id %><br>
    <%= f.number_field :tutor_id %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.datetime_select :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
