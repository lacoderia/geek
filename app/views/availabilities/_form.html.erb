<%= form_for(@availability) do |f| %>
  <% if @availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability.errors.count, "error") %> prohibited this availability from being saved:</h2>

      <ul>
      <% @availability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :week_day_id %><br>
    <%= f.number_field :week_day_id %>
  </div>
  <div class="field">
    <%= f.label :preference_id %><br>
    <%= f.number_field :preference_id %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.time_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.time_select :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
