<%= form_for(@registered_anomaly_status) do |f| %>
  <% if @registered_anomaly_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@registered_anomaly_status.errors.count, "error") %> prohibited this registered_anomaly_status from being saved:</h2>

      <ul>
      <% @registered_anomaly_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
